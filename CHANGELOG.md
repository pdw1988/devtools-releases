# 更新日志

## v1.5.1 —— 2026-09-21

· 连库查询「表结构」：去掉默认值和 Java 两列，字段名注释看得更全
  连库查询切「表结构」后，默认值列几乎全是 CURRENT_TIMESTAMP / SYSDATE 这类

· 连库查询 SQL 提示收窄：只有 `t.` 点号才列字段；选中片段可直接执行
  列名提示原本在 SELECT / WHERE / ORDER BY / 逗号 / 括号后面都往外糊，

· 连库查询内置 SQL 体检——结合索引与字段类型，给可执行的优化建议
  手写 SQL 好不好只有跑了才知道，等上量挨骂才发现。连库查询页新增

· 码上测：支持扫 jar 包——没有源码也能出接口库（fat jar 连依赖一起扫）
  没有源码、只有打好的 jar（外包交付/二方库/只拿到产物）时原来只能干瞪眼。

· 码上测：报告页失败一眼定位——归因贴脸、处理弹层直达、接口库 FAIL 一键跳报告
  反馈：报告页失败归因只有「鼠标悬停看处理建议」一条路，太粗糙；

· 码上测：压测/性能测试——四种模式真并发发压，TPS 分位实时看，连库还能盯数据库
  WebView 的 fetch 压不出真并发、计时也不准（P99 全失真），压测引擎

· 码上测：异常学习——接口说缺什么数据，自己补什么再重发
  反馈：代理商新增接口点测试，报「品牌商不在」——要用户自己处理前置

· 码上测：重复跑不再撞唯一键，备数只给正常用例，环境页再收一层
  对标业内自动化做法（Karate 的 callonce 每接口备一次、测试数据管理通行的

· 码上测：数据准备默认自动——不用找开关，生硬术语全改人话
  反馈：「不要用这么生硬的东西」「什么造数的确我都找不到」「大部分需要开

· 码上测：造数概念傻瓜化——「借/造/钉」三招 + 数据日志首次可见
  反馈：库前置数据「造了一堆概念」，开发和测试都看不懂。两个根因：

· 码上测：「接口运行和数据库是什么关系」——四步原理说明进产品
  反馈：连产品 owner 自己都说不清「接口运行 ↔ 数据库」的逻辑，用户更没法懂。

· 码上测：「默认全字段」——detailList 嵌套骨架不用每次手点
  正常用例的 body 默认是最小合法（{}，09-17 口径：回归不编造业务值，避免

· 码上测：接口库右栏重排——身份行 → 发送条 → 参数 → 结果，加 HUD 分区标签
  反馈是「摆放不太顺畅，能不能更科技点」。右栏首卡按一次 HTTP 事务的自然

· 码上测：树行只留类名 + 「试跑」更名「调试」+ 页面文案去文档编号
  ① 接口库树 Controller 行不再拼「类名 · 业务描述」——窄栏放不下，树上只放

· 码上测：界面层次感收口——主链路平铺、次级设置折叠且头部留当前值
  反馈是「一个页面东西太多，要条理和层次、逐步深化」。落法统一成三段式：

· 码上测：数据依赖收口——「未找到」配对用例 + 环境面板「样本值」入口
  ① 「异常·未找到」配对用例：getById 形态（必填 id 形参数）和写动词的 body id

· 码上测：依赖数据的接口不再空跑——读接口借库里真数据、删改打在自己造的行上
  详情/查询接口失败多半不是接口坏了，是库里没有那条数据：造的 id 不存在，

· 码上测：接口库 Controller 行不再显示作者·日期——悬浮可见
  左树 Controller 行原来三段：业务名 / 作者·日期 / 接口数。作者·日期那段在

· 码上测：点已扫描过的工程直接进接口库——不再停在扫描页
  首页「之前扫描过的工程」卡片和应用启动的「打开上次扫描结果」弹窗，

· 码上测：按 BizException 生成业务异常用例——用例名直接用后端代码里那句文案
  业务校验的正源在 Service 方法体里：if (dto.getBrandOwnerId() == null)

· 码上测：`@RequestMapping` 没写 method 的接口不再按 GET 发（带 `@RequestBody` 的就是 POST）
  ASCP /api/ascp/ctriporderpush/orderPush 一直被记成 500，报的是 Required

· 码上测：正常用例回归「最小合法」——非分页 body 发空对象、id 一律数字
  冒烟 body 全量填字段会把业务校验（BizException）路径全部跳过：字段都合法了，

· 码上测：HTTP 200 的业务失败不再记绿——用例可配响应体断言
  后端业务失败普遍走 BizException：HTTP 200 + {msg, code, level:"error"} 信封，

· 扫描页「实时发现」不要从头到尾挂着一句不动的话
  「实时发现」树和「工程队列」都是从 PROG 机器行**序列**还原的派生视图，

· 码上测：安装包里真的带上扫描器（补登记 `bundle.resources`）
  tauri.conf.json 的 bundle 块里没有 resources，src-tauri/resources/spring-scanner.jar

· 码上测：扫描器 jar 不再退回 Maven 产物（只认 `pnpm scanner:sync` 写下的那份）
  GUI（Rust apiforge.rs）和 CLI（cli/scanner.ts）都有一条「resources 里找不到

· 码上测：老用例里固化的 `test_demo` 重扫时会被刷掉（`id` 不再是占位值）
  上一版生成器对任何 String 字段一律发 test_demo，那个值被固化进了用例的

· 码上测：Controller 行尾的「只跑这个」按钮不再盖住接口数
  Controller 行的 ▶ 是绝对定位在行尾（right:2px、宽 22px），而接口数胶囊是该行 flex 的

· 码上测：jar 里的 DTO 字段保住泛型，`List<Long>` 不再退化成空数组
  依赖 jar 里的 DTO 只走字段描述符，而描述符是擦除后的 Ljava/util/List;，

· 码上测：用例值按字段含义出合法值、可开「库前置数据」自动造数并逆序清理（不接大模型）
  「AI 造用例」那个付费入口一直没实现，这轮按「不接大模型」的口径落地两件

· 码上测上专业版闸门：AI 上下文整包导出、批量回归、CI 无头运行要许可码，扫描浏览照旧免费
  商业化第 3 步：免费保留扫描/浏览、MD·HTML·OpenAPI 导出、单接口「▶发送」

· 扫描器认组合注解与继承链：自定义 @MyApi/@MyGet、基类前缀和基类接口不再漏
  以前只认内置的 @RestController/@Controller + 五个 Mapping 注解，两种

· 码上测定位一句话：一键扫出 Spring Boot 全部接口 · 喂给 AI 写单测 · 一键回归

· 接口库左树搜索优化：渲染预算 + 命中高亮 + 一键清空
  搜索 = 工程/Controller 全展开，几千接口全命中时一次渲染上万行 DOM，

· Java 17 工程的接口整批丢失：扫描器没设语言级别，record 全解析失败
  扫描器没设 JavaParser 语言级别（默认低于 JAVA_14），Java 17 项目满地的

· 一键复制 AI 上下文：扫描结果直接喂给 Cursor / Claude
  扫描结果新增第四种投影：AI 上下文——把接口清单压成扁平 Markdown（一行

· 回归范围：只跑某个服务/某个 Controller，报告带上范围
  以前一跑就是全部接口，改一个 Controller 也得全量陪跑。现在顶栏多了

· 用例 BODY 嵌套骨架：detailList 这类对象集合造出元素样例
  body 骨架只到第一层，List<GroupAgentApplyDetailAddCondition> 这类对象

· 试跑 BODY 显 {}：载缓存把 requestBody 剥没了（不用重扫）
  normalize 对 path 做了「原始/已归一」双形状兼容，requestBody 漏了同款：

· 左侧菜单重排：数据库功能收进「码上查」大菜单，APIForge 更名「码上测」
  数据库 11 页收进一个大菜单（马上查：表结构/生成 SQL/收藏/连库）；

· 扫描器拆类：一个 1600 行的上帝类拆成 20 个责任类，行为零漂移
  Scanner.java 一个类装了工程发现、文件收集、Java/XML 解析、DTO 来源兜底、

· 更名「码上 CodeNow」：安装包与界面换成新名字
  定位从「对着表出 SQL」升级为 Java 开发平台，旧名装不住盘；只改打包产物与

· 跨工程 DTO 解析：api 模块定义的请求类不再丢字段（891 个 DTO 获救）
  group-ui 的 Controller 引用 aier-ascp-api 里定义的 GroupAgentApplyAddCondition，

· 扫描能挖到任意深层的工程了：ascp/mobile 下的工程不再漏
  findProjectDirs 原来只看直接子目录：扫 ascp 根只出 8 个直属工程，

· APIForge 请求参数的 JSON 对象看得见了：试跑卡多行，发出去的请求体进报告
  之前 runCase 只存 URL 不存请求体：试跑卡 Body 是单行 JSON.stringify 一坨，

· APIForge Token 手输即生效：砍掉剪贴板按钮和两步注入
  用户原话：token 干脆手输吧，什么门户投、粘贴板，好麻烦。

· APIForge 执行顺序编排：新增（POST）先跑，顺序可配置
  只改组间先后：GET 仍并发 3、写动词全局串行，库变更归因不受影响。

· APIForge 关联表页头不再折行

· 连库查询能收藏 SQL；收藏详情一点就回到当时的库
  只灌不跑：dbQuerySeed 加 run 开关，缺省 true 保住三个旧入口。

· 窗口没放大时，页面底部的按钮不再被切
  顶栏 flex-wrap 会在窄窗口折成两行，calc(100vh - 132px) 里的 132

· 连库查询加上排序 / 分组 / 分页
  默认不分组、按主键（id）降序、一页 20 条。以前只能看库里碍事的前 200 行，

· 连库查询的条件区一行能放下 2-3 个字段
  条件格原先不管勾没勾都按 300px 留宽，可未勾的只有一个字段名，

· 本版暂不露出 APIForge 菜单
  功能代码仍在仓库；导航与 PAGES 暂退，bundle 不带 spring-scanner.jar。

· 连库查询字段/条件默认折起
  默认展开挡结果；要勾列再点开。查完仍不自动折（上次已修）。

· 连库结果区加「数据 / 表结构」切换（DBeaver 式）
  跟 DBeaver 一样在同一张结果卡里切页，表结构复用 ColumnTable，点表即有列清单。

· 连库字段/条件固定高度，查完不再自动折起
  原先查完结果会把条件卡折掉，刚勾的条件看不见；SQL/字段/条件各卡封高，

· 连库查询字段/条件字段排序跟单表一致
  拉列、勾选进 SELECT/WHERE 都走 sortColumnsForUi，跟单表勾选清单同一套，

· 批量 INSERT 字段规则默认展开，关键列外点加载更多
  其余字段点「加载更多」再出；卡本身默认不折。跟单表查询字段同一套分层。

· 多表条件字段跟单表一样：默认值 + 比较符
  原先只有勾选 + #{}；现在格子里带操作符和默认值，列表 SQL 可直接跑。

· 多表查询列/条件列改两列加高，默认只勾关键列
  左栏四列挤成「一行只看见两三个、字被裁、156px 再裁掉」；改成两列+加高。

· （已回退）初始窗口下最后一排按钮要往下拉

· 多表关联的左表/右表不再裁掉半截名字
  一行里两个下拉各占一半，T_ASCP_VIEW_… 这种长标识符必然被省略号裁掉；

· 查询字段不再默认勾上没展开的列
  原先未动＝全选，收起的列看不见却照样进 SQL；现在未动＝只勾默认露出的那批，

· COUNT(*) 和 COUNT(主键) 挨着放
  两项原先各占四列网格的 2 格，格子比文字宽出一大截，中间空一片；

> Windows 安装包（推荐）：`CodeNow_1.5.1_x64-setup.exe` · 含 Oracle 11g 客户端：GitHub Release `v1.5.1` 附件 `CHUJU-SQL_1.5.1_x64-setup-oracle11g.exe`（超 100MB，不进 git） · 便携版：`CodeNow_1.5.1_portable.exe`（免安装；不含自动更新）
> macOS：`CodeNow_1.5.1_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）

## v1.5.0 —— 2026-09-09

· 窗口没放大时，页面底部的按钮不再被切
  顶栏 flex-wrap 会在窄窗口折成两行，calc(100vh - 132px) 里的 132

· 连库查询加上排序 / 分组 / 分页
  默认不分组、按主键（id）降序、一页 20 条。以前只能看库里碍事的前 200 行，

· 连库查询的条件区一行能放下 2-3 个字段
  条件格原先不管勾没勾都按 300px 留宽，可未勾的只有一个字段名，

· 本版暂不露出 APIForge 菜单
  功能代码仍在仓库；导航与 PAGES 暂退，bundle 不带 spring-scanner.jar。

· 连库查询字段/条件默认折起
  默认展开挡结果；要勾列再点开。查完仍不自动折（上次已修）。

· 连库结果区加「数据 / 表结构」切换（DBeaver 式）
  跟 DBeaver 一样在同一张结果卡里切页，表结构复用 ColumnTable，点表即有列清单。

· 连库字段/条件固定高度，查完不再自动折起
  原先查完结果会把条件卡折掉，刚勾的条件看不见；SQL/字段/条件各卡封高，

· 连库查询字段/条件字段排序跟单表一致
  拉列、勾选进 SELECT/WHERE 都走 sortColumnsForUi，跟单表勾选清单同一套，

· 批量 INSERT 字段规则默认展开，关键列外点加载更多
  其余字段点「加载更多」再出；卡本身默认不折。跟单表查询字段同一套分层。

· 多表条件字段跟单表一样：默认值 + 比较符
  原先只有勾选 + #{}；现在格子里带操作符和默认值，列表 SQL 可直接跑。

· 多表查询列/条件列改两列加高，默认只勾关键列
  左栏四列挤成「一行只看见两三个、字被裁、156px 再裁掉」；改成两列+加高。

· （已回退）初始窗口下最后一排按钮要往下拉

· 多表关联的左表/右表不再裁掉半截名字
  一行里两个下拉各占一半，T_ASCP_VIEW_… 这种长标识符必然被省略号裁掉；

· 查询字段不再默认勾上没展开的列
  原先未动＝全选，收起的列看不见却照样进 SQL；现在未动＝只勾默认露出的那批，

· COUNT(*) 和 COUNT(主键) 挨着放
  两项原先各占四列网格的 2 格，格子比文字宽出一大截，中间空一片；

> Windows 安装包（推荐）：`CHUJU SQL_1.5.0_x64-setup.exe` · 含 Oracle 11g 客户端：GitHub Release `v1.5.0` 附件 `CHUJU-SQL_1.5.0_x64-setup-oracle11g.exe`（超 100MB，不进 git） · 便携版：`CHUJU-SQL_1.5.0_portable.exe`（免安装；不含自动更新）
> macOS：`CHUJU SQL_1.5.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）
## v1.4.0 —— 2026-09-04

· 连库已连接自动最大化
  原先只靠 CSS 藏 Fragment 下的 .top，且 connected 未及时写入 store，看起来像没最大化。

· 连库结果底栏按钮不再被裁掉
  SQL 勾列一高会把复制/插入/更新/删除顶出窗口。有结果后两块默认折起，按钮进网格底栏；列头去掉序号。

· 连库结果列少时铺满视口
  内容估宽为底；视口有余量再分。列多仍横滚，不挤缝。

· 多表关联可勾选条件字段
  默认主表主键；全不选只留 WHERE 1=1；占位 #{别名_javaField} 避免撞名。

· 多表列表支持条数、排序、分组
  分页写成字面量；分组时列表变为列+COUNT(*)，查询列勾选对列表暂不生效。

· 多表关联可勾选查询列
  默认全选；全不选仍留主键和 JOIN 键。结果仍在右栏最上。

· 单表排序/分组下拉对齐
  条数和「排序」之间留空；没选表不再是空白下拉。

· 记住密码勾选点了没反应
  WebView 里自定义 checkbox 有时不触发 change，改成 label 自己切。

· Ctrl+Shift+D 全局呼出，关窗口进托盘
  关掉窗口不退出，托盘「退出」才真关。快捷键被占用时不影响启动。

· 换成 CHUJU SQL 自己的应用图标
  桌面安装后不再是青黄双环；霜晶蓝底 + 表结构→SQL，源稿在 scripts/icon-source.svg。

· 单表可执行 SELECT 支持排序和分组
  默认同预编译主键 DESC；可选排序与分组。分组时 SELECT 改为维度列+COUNT(*)，避免严格模式拒跑。

· 产品方言加回 SQLite
  生成 / 批量 / 导出 / 模板与三种库并列；多 VALUES 对 SQLite 开放。连库直连仍不做。

· 终端磷光底色淡一点
  长时间对 SQL 纯黑太刺，磷绿描线不动。

· 多表换主表时关联左表跟着变
  只改左表等于旧主表的行；手改过左表的不动。字段名能对上就保留。

· 批量页字段规则默认折起
  缺省自动值已够用，长规则表别挡右栏结果。

· 我的SQL库：左新建、右先搜后详情
  点收藏后详情/编辑在列表下面，不盖住搜索；新建草稿与编辑互不冲。

· SELECT转UPDATE 贴完能返回选表
  原先顶栏「清空」语义不清；改成「返回选表」，工作区左卡也放一份。

· 产品气质改成办公浅色
  默认霜晶中性灰底平面白卡片；深空同样平面。暗色仍可选，选择器右半边。

· 深空霓虹改成浅灰底
  同事吐槽闷。底改冷灰、字改深色，霓虹青强调保留；scheme 改 light。

· 默认主题改成浅色霜晶
  同事吐槽深空默认闷。新装/清缓存进浅色；暗色仍可选手动切。

· 连库查询 SQL 卡加条件字段
  点表仍 SELECT *；勾条件拼 WHERE（空值 IS NULL）。改值回车再查。列头筛以后再说。

· 查看表结构可跳去连库查数据
  编辑表结构旁边加按钮，带 SELECT * 进连库查询，已连库直接跑。

· 我的连接左栏独占新建表单
  去掉折叠和顶栏说明带；连库/已保存挪右栏，列表在卡内自滚。

· 我的连接左右栏对调
  已保存与右侧表单对调，空态文案同步。

· 我的SQL库左右栏对调
  搜索与新建收藏左右对调，空态文案同步。

· 侧栏缓存区只留表数和操作
  去掉 Tauri/sqlite、体积、更新时间，底栏再矮一截。

· 左侧「我的SQL库」不被底栏挡住
  生成 SQL 组最底下会被 ↻ 刷新顶出视口；独立一级并收紧导航/缓存区高度。

· 导入页左右栏等高、空态与左栏对齐
  右栏「等待建表语句」改用 Card.fill 同构，虚线区与左侧 textarea 同起止。

· 左侧菜单空滚动条（顶底钉死）
  整栏 overflow:auto + 底脚 margin-top:auto 会空出一条条。顶底钉死，中间不够高再滚（滚动条藏掉）。

· 撤回导入铺满，侧栏恢复原样
  Page fill 把左侧菜单搞出滚动条、「我的SQL库」被藏。只保留解析表默认折起。

· 左侧菜单去掉空滚动条
  整栏 overflow:auto + 底脚 margin-top:auto 会空出一条条。顶底钉住，中间不够高再滚。

· 仅导入粘贴页铺满，解析表默认折起
  只动导入→粘贴建表；左右对半吃满视口，字段表默认折叠。不改别的菜单页。

· 生成结果加上收藏
  单表 / 多表 / 批量 / SELECT转UPDATE 的 SQL 能一键进我的SQL库，同内容不重复建。

· 单表条件常开、模板默认展开，并加上条数
  条件一行两个不折起，模板进来就开着，左栏才饱满。SELECT 条数 10/20/50/100/500。

· 单表查询字段四列，条件加上 LIKE
  左栏加宽，全选按钮不再被裁。查询字段一行四个，条件一行两个。LIKE 没写通配符时自动包成 %值%。

· Oracle 分页改回 ROWNUM
  FETCH NEXT 是 12c 才有的，11g 会 ORA-00933，看起来也不像 Oracle。

· 单表 SQL：SELECT 在前，语句铺满右侧
  SELECT 排最前。查询字段和条件字段改成连库那种一行多个。条件带等于/不等于/大于等于。

· 表结构注释能看见，长度不再带 CHAR
  VARCHAR2(100 CHAR) 的 CHAR 是 Oracle 计长单位，不该出现在长度框。AI 改成自增。

· 加载一万行不再把窗口卡死
  一万行 × 很多列全进 DOM 会把窗口卡死。先出遮罩，表格只渲染视口附近。

· 查询结果行号看得见，列宽可拖
  200 列时浏览器把 # 列挤没。改成固定列宽，表头带列序号，右边拖。

· 连库查询勾列回来，底栏按钮不再被裁
  结果卡不能挪到最上面。勾列一直开着。SQL 卡限高，多字段在网格里滚。

· 勾了记住密码，重启应用还在
  Windows 改成本机加密文件，写失败会提示。不再悄悄丢掉。

· 「粘贴 ALTER」改名为「执行 ALTER」

· 测试/本地可跨天记住密码
  连接列表和 cache.db 仍没有密码。生产不记。关掉窗口明天还能连。

· 粘贴 ALTER 放到运行旁边
  中间那张卡再折着也占高度。改成 SQL 卡上的次要按钮，点开弹窗再贴。

· 更新弹窗带上这次改了什么
  以前 latest.json 常只剩「发版 vX」，用户看不见内容。未发版的 CHANGES.md 自动写进说明；弹窗里可滚动。

· 粘贴 ALTER 展开后结果网格横条没了
  右栏 overflow:auto 两轴一起开，展开的 ALTER 把栏撑宽，结果表跟着撑满，横条没了。

· 拿掉界面上的需求确认文案
  范围清单是给评审看的，不该印在连接页和确认框里。按钮和 SQL 还在，只是不再念一遍「做什么、不做什么」。

· 选表下拉可搜索
  生成页、批量、SELECT转UPDATE、多表 JOIN 不再用原生 select 翻清单。

· 连库插入一行（PRD 19.7）
  独立写命令，SQL 框仍然只读。自增列默认不写。不拿复制 INSERT 在本表执行。

· 连库勾选行按主键更新（PRD 19.4）
  独立写命令，SQL 框仍然只读。各行同一套 SET。没主键、没勾字段都不改。

· 生成页在当前连接执行（PRD 19.5）
  单表 / 批量 / SELECT转UPDATE 确认后执行。没连上或方言对不上就禁用。

· 连库勾选行按主键删除（PRD 19.3）
  独立写命令，SQL 框仍然只读。没主键、对不上列、空主键都不删。

· 连库查询写入 PRD §十九，发版 0.9.0
  Oracle / MySQL / PostgreSQL 点表即查；密码不落盘；SQL 框不跑写操作。

· 连库查询 SQL 框铺满右栏宽度

· 连库查询：SQL 在上、结果在下

· 左侧导航收成三组

· 连库工作台：左栏不再空出一截

· 连库表单：列名在输入框前面

· 连库接入 PostgreSQL（共用壳上的第三条方言）
  - 自写 postgres_* 命令，不走 plugin-sql；页面仍用共用壳

· 真页面：我的连接 + 连库进度

· 原型：未连接恢复手填表单

· 原型：连库查询先选「我的连接」

· 原型：我的连接

· 连库后收起左侧菜单给表清单让宽

· 连库 IPC 不再动态 import Tauri 包

· 连库查询独立成一级菜单和工作台
  - 导入页不再嵌「连接数据库」Seg

· 连库后只读 SQL 查询
  - 先 200 行，加载全部最多 1 万；点列头排序、结果内筛选

· 许可码改走 Rust Ed25519
  - 私钥只在 ~/.tauri/chuju-license.key；安装包只有公钥

· macOS 发版补打 .app.tar.gz

· 删掉旧 SqlGenerator，修打包 TS2322

· 文档：11g 连库要 Instant Client，不必装 PL/SQL

· 免费 / 专业版闸门
  - 自导表最多 8 张；SELECT转UPDATE / 多表 / 批量 / 导出 DDL 能看不能拷

· 连库接入 MySQL（共用壳上的第二条方言）
  - 自写 mysql_* 命令，不走 plugin-sql；页面仍用共用壳

· 连库页抽成共用壳，方言只分连接和字典
  - 接入 MySQL 写 src/lib/dbDialects/mysql.ts 并替换 DIALECTS 槽位，不要改 DbConnect.tsx

· Oracle 只读连库导入
  - 官方纯 Rust 驱动，不经过 plugin-sql；密码不落盘

· 打包增加 macOS DMG
  - Mac 上 tauri build 出 dmg；Windows 仍是 NSIS

· SELECT转UPDATE 列名大小写和列数对不上
  - 表头小写对不上大写主键时 WHERE 不再是空的

· 产品显示名改为 CHUJU SQL
  - 窗口、侧栏、安装包显示名对齐；identifier 和缓存 key 不动

· 左侧导航按使用顺序收成一级菜单
  - 日志转SQL、SELECT转UPDATE、我的SQL库仍是独立一级入口

· 批量 INSERT 主键/唯一列连号，不再走自动值
  - 散列自动值会撞 UNIQUE，语句能生成、插不进去

· SELECT转UPDATE 网格粘贴不再被 JSON 里的竖线拆碎
  - 流程实例一类带 JSON 的查询结果不再整行错位

· SELECT转UPDATE 必须先选表再粘贴
  - 有列头按列名对齐，没列头按这张表的字段顺序对齐

· 批量生成正式页拆成 INSERT / UPDATE 两页并加回导航
  - 右栏第一张是生成结果；预览默认折起

· SELECT转UPDATE 正式页按 mockup 重排并加回导航
  - 没贴时只留粘贴；贴完右栏第一张是生成结果，预览默认折起

· 批量生成原型拆成 INSERT / UPDATE 两页
  - 导航「批量生成」作分组；旧 batch-gen.html 转到 INSERT

· 多表关联切菜单再进来会刷新
  - 再点一次当前「多表关联」也当作重新进来

· 产品方言暂时去掉 SQLite
  - 旧模板若存了 sqlite 整条丢掉，避免套用时对不上

· 模板必须点「套用」按钮，点行不再套用
  - 扫一眼名字或点删除附近时，不会把当前配置冲掉

· 多表添加关联时表名不再裁成半截
  - 左表/右表分行；弹层 portal 到 body，长表名折行，表多时可筛选

· 代码块复制仍在顶上，下载和下方复制给长语句
  - 短语句不滚就能复制；正文会在框里滚时脚上再给一次复制

· 日志转SQL「复制全部纯 SQL」按钮上提示已复制
  - 反馈在按钮上，不弹角落 toast

· 日志转SQL 按语句骨架自动选 Oracle / MySQL / PostgreSQL 字面量
  - 反引号按 MySQL 拼日期；NVL/SYSDATE 按 Oracle；ILIKE/:: 按 PostgreSQL

· 日志转SQL 去掉方言切换
  - 字面量固定按 Oracle 拼；顶栏分段和「换成 Oracle / MySQL」一并拿掉

· SQL库长表名不再被裁切
  - 列表名、所属表、按表名筛选都能看见全名

· 日志转SQL 收藏后切到我的SQL库立即可见
  - 各页同时挂着不卸载，收藏只写存储的话 SQL 库仍显示挂载时读到的旧列表

· SQL库右栏按意图互斥：新建或详情，不同时出现
  - 未选中时右侧是新建；点列表后换成详情复制；卡片脚「返回新建」回到表单

· 导航各页书面用语 + 产物优先 + 表头不再挡行
  - 多表模板默认折起；日志对照表默认折起、hero 收短；SQL库右栏先详情复制

· 单表 SQL：语句类型一次只出一种，不再四种 CRUD 混成一段
  - 勾选改成分段选择：点 SELECT 结果里就只有 SELECT，复制出去不用再自己拆

· 编辑表结构弹窗一打开就炸（类型列拆装字段名对不上）
  - toRows 原先把 { base, args } 直接 spread 进行，TypePick 读的是 typeBase，

· 我的SQL库：粘贴 SQL 即识别表名
  - 所属表名跟着 SQL 走（PRD §6.4 / §6.7），手改过表名框后不再覆盖

· 导出 DDL：DEFAULT 放到 NOT NULL 前面
  - columnParts 统一成「类型 → DEFAULT/IDENTITY → NOT NULL → 注释」

· 日志转SQL 结果卡一键收藏 · 「我的SQL库」恢复进导航
  - 每张「解析结果」卡的按钮排加「⭐ 收藏」：整卡语句存成一条收藏，

· 修云端发布构建：@tauri-apps/api 补成直接依赖
  - UpdateButton.tsx 动态 import('@tauri-apps/api/app')，但 package.json 里只有

· 修导入：全引号形态的 Oracle DDL 整批报废（导出→再导入的回路是断的）
  - node-sql-parser 的 mysql 语法把

· 编辑表结构弹窗实时生成增量 DDL（ALTER TABLE）
  - 弹窗字段表格下方新增「增量 DDL」区：拿打开弹窗时的字段和当前编辑实时 diff，

· 菜单暂只留三条：导入表结构 / 查看表结构 / 日志转SQL
  - 生成SQL / SQL语句转换 / 批量生成SQL / 我的SQL库 从左侧菜单拿掉（用户要求

· 编辑表结构：类型列加常用类型下拉 · 补种子 Java 类名显式赋值
  - 类型输入框旁常驻 ▾，点开弹常用类型清单，点选即填；照旧可以手输任意类型

· 导入页「干净态」改版：初始全空 · 导入后结果留页
  - 初始态：输入框不再预填 Oracle 示例，右栏换成虚线空态占位（.empty-hero），

· Oracle 导出 DDL：主键走独立的 alter table add constraint
  - 以前主键只以内联 PRIMARY KEY (id) 写在 CREATE TABLE 体内：约束不会丢，

· 示例表名统一大写 ORDER（速查页两张订单表的根因）· 速查卡加导入日期
  - 根因：缓存按名字原文区分大小写。种子叫小写 order、Oracle 示例导进来是

· 导航重排：配置管理暂退，七页改名并按使用顺序排列
  - 配置管理从导航拿掉（功能代码保留，PAGES 数组加回条目即恢复，

· 导入页：去掉「导入之前你该知道的」，建表输入框伸到底
  - 删「导入之前你该知道的」卡：三条说明里两条已在别处出现

· 修 bug9 速查残留内置表 · bug10 表结构可删除
  - 新增 storage/builtinSeed.ts 启动同步：凭「种子时间戳 + 退役名单」清掉

· 全项目示例只留「订单 + 订单明细」
  - 种子数据、导入页两段 DDL、日志转SQL 的 5 条样例、正则测试预设里的表名统一成

> Windows 安装包（推荐）：`CHUJU SQL_1.4.0_x64-setup.exe` · 含 Oracle 11g 客户端：GitHub Release `v1.4.0` 附件 `CHUJU-SQL_1.4.0_x64-setup-oracle11g.exe`（超 100MB，不进 git） · 便携版：`CHUJU-SQL_1.4.0_portable.exe`（免安装；不含自动更新）
> macOS：`CHUJU SQL_1.4.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v1.3.0 —— 2026-09-02

· 终端磷光底色淡一点
  长时间对 SQL 纯黑太刺，磷绿描线不动。

· 多表换主表时关联左表跟着变
  只改左表等于旧主表的行；手改过左表的不动。字段名能对上就保留。

· 批量页字段规则默认折起
  缺省自动值已够用，长规则表别挡右栏结果。

· 我的SQL库：左新建、右先搜后详情
  点收藏后详情/编辑在列表下面，不盖住搜索；新建草稿与编辑互不冲。

· SELECT转UPDATE 贴完能返回选表
  原先顶栏「清空」语义不清；改成「返回选表」，工作区左卡也放一份。

· 产品气质改成办公浅色
  默认霜晶中性灰底平面白卡片；深空同样平面。暗色仍可选，选择器右半边。

· 深空霓虹改成浅灰底
  同事吐槽闷。底改冷灰、字改深色，霓虹青强调保留；scheme 改 light。

· 默认主题改成浅色霜晶
  同事吐槽深空默认闷。新装/清缓存进浅色；暗色仍可选手动切。

· 连库查询 SQL 卡加条件字段
  点表仍 SELECT *；勾条件拼 WHERE（空值 IS NULL）。改值回车再查。列头筛以后再说。

· 查看表结构可跳去连库查数据
  编辑表结构旁边加按钮，带 SELECT * 进连库查询，已连库直接跑。

· 我的连接左栏独占新建表单
  去掉折叠和顶栏说明带；连库/已保存挪右栏，列表在卡内自滚。

· 我的连接左右栏对调
  已保存与右侧表单对调，空态文案同步。

· 我的SQL库左右栏对调
  搜索与新建收藏左右对调，空态文案同步。

· 侧栏缓存区只留表数和操作
  去掉 Tauri/sqlite、体积、更新时间，底栏再矮一截。

· 左侧「我的SQL库」不被底栏挡住
  生成 SQL 组最底下会被 ↻ 刷新顶出视口；独立一级并收紧导航/缓存区高度。

· 导入页左右栏等高、空态与左栏对齐
  右栏「等待建表语句」改用 Card.fill 同构，虚线区与左侧 textarea 同起止。

· 左侧菜单空滚动条（顶底钉死）
  整栏 overflow:auto + 底脚 margin-top:auto 会空出一条条。顶底钉死，中间不够高再滚（滚动条藏掉）。

· 撤回导入铺满，侧栏恢复原样
  Page fill 把左侧菜单搞出滚动条、「我的SQL库」被藏。只保留解析表默认折起。

· 左侧菜单去掉空滚动条
  整栏 overflow:auto + 底脚 margin-top:auto 会空出一条条。顶底钉住，中间不够高再滚。

· 仅导入粘贴页铺满，解析表默认折起
  只动导入→粘贴建表；左右对半吃满视口，字段表默认折叠。不改别的菜单页。

· 生成结果加上收藏
  单表 / 多表 / 批量 / SELECT转UPDATE 的 SQL 能一键进我的SQL库，同内容不重复建。

· 单表条件常开、模板默认展开，并加上条数
  条件一行两个不折起，模板进来就开着，左栏才饱满。SELECT 条数 10/20/50/100/500。

· 单表查询字段四列，条件加上 LIKE
  左栏加宽，全选按钮不再被裁。查询字段一行四个，条件一行两个。LIKE 没写通配符时自动包成 %值%。

· Oracle 分页改回 ROWNUM
  FETCH NEXT 是 12c 才有的，11g 会 ORA-00933，看起来也不像 Oracle。

· 单表 SQL：SELECT 在前，语句铺满右侧
  SELECT 排最前。查询字段和条件字段改成连库那种一行多个。条件带等于/不等于/大于等于。

· 表结构注释能看见，长度不再带 CHAR
  VARCHAR2(100 CHAR) 的 CHAR 是 Oracle 计长单位，不该出现在长度框。AI 改成自增。

· 加载一万行不再把窗口卡死
  一万行 × 很多列全进 DOM 会把窗口卡死。先出遮罩，表格只渲染视口附近。

· 查询结果行号看得见，列宽可拖
  200 列时浏览器把 # 列挤没。改成固定列宽，表头带列序号，右边拖。

· 连库查询勾列回来，底栏按钮不再被裁
  结果卡不能挪到最上面。勾列一直开着。SQL 卡限高，多字段在网格里滚。

· 勾了记住密码，重启应用还在
  Windows 改成本机加密文件，写失败会提示。不再悄悄丢掉。

· 「粘贴 ALTER」改名为「执行 ALTER」

· 测试/本地可跨天记住密码
  连接列表和 cache.db 仍没有密码。生产不记。关掉窗口明天还能连。

· 粘贴 ALTER 放到运行旁边
  中间那张卡再折着也占高度。改成 SQL 卡上的次要按钮，点开弹窗再贴。

· 更新弹窗带上这次改了什么
  以前 latest.json 常只剩「发版 vX」，用户看不见内容。未发版的 CHANGES.md 自动写进说明；弹窗里可滚动。

· 粘贴 ALTER 展开后结果网格横条没了
  右栏 overflow:auto 两轴一起开，展开的 ALTER 把栏撑宽，结果表跟着撑满，横条没了。

· 拿掉界面上的需求确认文案
  范围清单是给评审看的，不该印在连接页和确认框里。按钮和 SQL 还在，只是不再念一遍「做什么、不做什么」。

· 选表下拉可搜索
  生成页、批量、SELECT转UPDATE、多表 JOIN 不再用原生 select 翻清单。

· 连库插入一行（PRD 19.7）
  独立写命令，SQL 框仍然只读。自增列默认不写。不拿复制 INSERT 在本表执行。

· 连库勾选行按主键更新（PRD 19.4）
  独立写命令，SQL 框仍然只读。各行同一套 SET。没主键、没勾字段都不改。

· 生成页在当前连接执行（PRD 19.5）
  单表 / 批量 / SELECT转UPDATE 确认后执行。没连上或方言对不上就禁用。

· 连库勾选行按主键删除（PRD 19.3）
  独立写命令，SQL 框仍然只读。没主键、对不上列、空主键都不删。

· 连库查询写入 PRD §十九，发版 0.9.0
  Oracle / MySQL / PostgreSQL 点表即查；密码不落盘；SQL 框不跑写操作。

· 连库查询 SQL 框铺满右栏宽度

· 连库查询：SQL 在上、结果在下

· 左侧导航收成三组

· 连库工作台：左栏不再空出一截

· 连库表单：列名在输入框前面

· 连库接入 PostgreSQL（共用壳上的第三条方言）
  - 自写 postgres_* 命令，不走 plugin-sql；页面仍用共用壳

· 真页面：我的连接 + 连库进度

· 原型：未连接恢复手填表单

· 原型：连库查询先选「我的连接」

· 原型：我的连接

· 连库后收起左侧菜单给表清单让宽

· 连库 IPC 不再动态 import Tauri 包

· 连库查询独立成一级菜单和工作台
  - 导入页不再嵌「连接数据库」Seg

· 连库后只读 SQL 查询
  - 先 200 行，加载全部最多 1 万；点列头排序、结果内筛选

· 许可码改走 Rust Ed25519
  - 私钥只在 ~/.tauri/chuju-license.key；安装包只有公钥

· macOS 发版补打 .app.tar.gz

· 删掉旧 SqlGenerator，修打包 TS2322

· 文档：11g 连库要 Instant Client，不必装 PL/SQL

· 免费 / 专业版闸门
  - 自导表最多 8 张；SELECT转UPDATE / 多表 / 批量 / 导出 DDL 能看不能拷

· 连库接入 MySQL（共用壳上的第二条方言）
  - 自写 mysql_* 命令，不走 plugin-sql；页面仍用共用壳

· 连库页抽成共用壳，方言只分连接和字典
  - 接入 MySQL 写 src/lib/dbDialects/mysql.ts 并替换 DIALECTS 槽位，不要改 DbConnect.tsx

· Oracle 只读连库导入
  - 官方纯 Rust 驱动，不经过 plugin-sql；密码不落盘

· 打包增加 macOS DMG
  - Mac 上 tauri build 出 dmg；Windows 仍是 NSIS

· SELECT转UPDATE 列名大小写和列数对不上
  - 表头小写对不上大写主键时 WHERE 不再是空的

· 产品显示名改为 CHUJU SQL
  - 窗口、侧栏、安装包显示名对齐；identifier 和缓存 key 不动

· 左侧导航按使用顺序收成一级菜单
  - 日志转SQL、SELECT转UPDATE、我的SQL库仍是独立一级入口

· 批量 INSERT 主键/唯一列连号，不再走自动值
  - 散列自动值会撞 UNIQUE，语句能生成、插不进去

· SELECT转UPDATE 网格粘贴不再被 JSON 里的竖线拆碎
  - 流程实例一类带 JSON 的查询结果不再整行错位

· SELECT转UPDATE 必须先选表再粘贴
  - 有列头按列名对齐，没列头按这张表的字段顺序对齐

· 批量生成正式页拆成 INSERT / UPDATE 两页并加回导航
  - 右栏第一张是生成结果；预览默认折起

· SELECT转UPDATE 正式页按 mockup 重排并加回导航
  - 没贴时只留粘贴；贴完右栏第一张是生成结果，预览默认折起

· 批量生成原型拆成 INSERT / UPDATE 两页
  - 导航「批量生成」作分组；旧 batch-gen.html 转到 INSERT

· 多表关联切菜单再进来会刷新
  - 再点一次当前「多表关联」也当作重新进来

· 产品方言暂时去掉 SQLite
  - 旧模板若存了 sqlite 整条丢掉，避免套用时对不上

· 模板必须点「套用」按钮，点行不再套用
  - 扫一眼名字或点删除附近时，不会把当前配置冲掉

· 多表添加关联时表名不再裁成半截
  - 左表/右表分行；弹层 portal 到 body，长表名折行，表多时可筛选

· 代码块复制仍在顶上，下载和下方复制给长语句
  - 短语句不滚就能复制；正文会在框里滚时脚上再给一次复制

· 日志转SQL「复制全部纯 SQL」按钮上提示已复制
  - 反馈在按钮上，不弹角落 toast

· 日志转SQL 按语句骨架自动选 Oracle / MySQL / PostgreSQL 字面量
  - 反引号按 MySQL 拼日期；NVL/SYSDATE 按 Oracle；ILIKE/:: 按 PostgreSQL

· 日志转SQL 去掉方言切换
  - 字面量固定按 Oracle 拼；顶栏分段和「换成 Oracle / MySQL」一并拿掉

· SQL库长表名不再被裁切
  - 列表名、所属表、按表名筛选都能看见全名

· 日志转SQL 收藏后切到我的SQL库立即可见
  - 各页同时挂着不卸载，收藏只写存储的话 SQL 库仍显示挂载时读到的旧列表

· SQL库右栏按意图互斥：新建或详情，不同时出现
  - 未选中时右侧是新建；点列表后换成详情复制；卡片脚「返回新建」回到表单

· 导航各页书面用语 + 产物优先 + 表头不再挡行
  - 多表模板默认折起；日志对照表默认折起、hero 收短；SQL库右栏先详情复制

· 单表 SQL：语句类型一次只出一种，不再四种 CRUD 混成一段
  - 勾选改成分段选择：点 SELECT 结果里就只有 SELECT，复制出去不用再自己拆

· 编辑表结构弹窗一打开就炸（类型列拆装字段名对不上）
  - toRows 原先把 { base, args } 直接 spread 进行，TypePick 读的是 typeBase，

· 我的SQL库：粘贴 SQL 即识别表名
  - 所属表名跟着 SQL 走（PRD §6.4 / §6.7），手改过表名框后不再覆盖

· 导出 DDL：DEFAULT 放到 NOT NULL 前面
  - columnParts 统一成「类型 → DEFAULT/IDENTITY → NOT NULL → 注释」

· 日志转SQL 结果卡一键收藏 · 「我的SQL库」恢复进导航
  - 每张「解析结果」卡的按钮排加「⭐ 收藏」：整卡语句存成一条收藏，

· 修云端发布构建：@tauri-apps/api 补成直接依赖
  - UpdateButton.tsx 动态 import('@tauri-apps/api/app')，但 package.json 里只有

· 修导入：全引号形态的 Oracle DDL 整批报废（导出→再导入的回路是断的）
  - node-sql-parser 的 mysql 语法把

· 编辑表结构弹窗实时生成增量 DDL（ALTER TABLE）
  - 弹窗字段表格下方新增「增量 DDL」区：拿打开弹窗时的字段和当前编辑实时 diff，

· 菜单暂只留三条：导入表结构 / 查看表结构 / 日志转SQL
  - 生成SQL / SQL语句转换 / 批量生成SQL / 我的SQL库 从左侧菜单拿掉（用户要求

· 编辑表结构：类型列加常用类型下拉 · 补种子 Java 类名显式赋值
  - 类型输入框旁常驻 ▾，点开弹常用类型清单，点选即填；照旧可以手输任意类型

· 导入页「干净态」改版：初始全空 · 导入后结果留页
  - 初始态：输入框不再预填 Oracle 示例，右栏换成虚线空态占位（.empty-hero），

· Oracle 导出 DDL：主键走独立的 alter table add constraint
  - 以前主键只以内联 PRIMARY KEY (id) 写在 CREATE TABLE 体内：约束不会丢，

· 示例表名统一大写 ORDER（速查页两张订单表的根因）· 速查卡加导入日期
  - 根因：缓存按名字原文区分大小写。种子叫小写 order、Oracle 示例导进来是

· 导航重排：配置管理暂退，七页改名并按使用顺序排列
  - 配置管理从导航拿掉（功能代码保留，PAGES 数组加回条目即恢复，

· 导入页：去掉「导入之前你该知道的」，建表输入框伸到底
  - 删「导入之前你该知道的」卡：三条说明里两条已在别处出现

· 修 bug9 速查残留内置表 · bug10 表结构可删除
  - 新增 storage/builtinSeed.ts 启动同步：凭「种子时间戳 + 退役名单」清掉

· 全项目示例只留「订单 + 订单明细」
  - 种子数据、导入页两段 DDL、日志转SQL 的 5 条样例、正则测试预设里的表名统一成

> Windows 安装包（推荐）：`CHUJU SQL_1.3.0_x64-setup.exe` · 含 Oracle 11g 客户端：GitHub Release `v1.3.0` 附件 `CHUJU-SQL_1.3.0_x64-setup-oracle11g.exe`（超 100MB，不进 git） · 便携版：`CHUJU-SQL_1.3.0_portable.exe`（免安装；不含自动更新）
> macOS：`CHUJU SQL_1.3.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v1.2.0 —— 2026-09-02

· 我的连接右栏已保存与连库对调
  选连接在上、填密码连库在下，空态文案同步。

· 我的连接左栏独占新建表单
  去掉折叠和顶栏说明带；连库/已保存挪右栏，列表在卡内自滚。

· 我的连接左右栏对调
  已保存与右侧表单对调，空态文案同步。

· 我的SQL库左右栏对调
  搜索与新建收藏左右对调，空态文案同步。

· 侧栏缓存区只留表数和操作
  去掉 Tauri/sqlite、体积、更新时间，底栏再矮一截。

· 左侧「我的SQL库」不被底栏挡住
  生成 SQL 组最底下会被 ↻ 刷新顶出视口；独立一级并收紧导航/缓存区高度。

· 导入页左右栏等高、空态与左栏对齐
  右栏「等待建表语句」改用 Card.fill 同构，虚线区与左侧 textarea 同起止。

· 左侧菜单空滚动条（顶底钉死）
  整栏 overflow:auto + 底脚 margin-top:auto 会空出一条条。顶底钉死，中间不够高再滚（滚动条藏掉）。

· 撤回导入铺满，侧栏恢复原样
  Page fill 把左侧菜单搞出滚动条、「我的SQL库」被藏。只保留解析表默认折起。

· 左侧菜单去掉空滚动条
  整栏 overflow:auto + 底脚 margin-top:auto 会空出一条条。顶底钉住，中间不够高再滚。

· 仅导入粘贴页铺满，解析表默认折起
  只动导入→粘贴建表；左右对半吃满视口，字段表默认折叠。不改别的菜单页。

· 生成结果加上收藏
  单表 / 多表 / 批量 / SELECT转UPDATE 的 SQL 能一键进我的SQL库，同内容不重复建。

· 单表条件常开、模板默认展开，并加上条数
  条件一行两个不折起，模板进来就开着，左栏才饱满。SELECT 条数 10/20/50/100/500。

· 单表查询字段四列，条件加上 LIKE
  左栏加宽，全选按钮不再被裁。查询字段一行四个，条件一行两个。LIKE 没写通配符时自动包成 %值%。

· Oracle 分页改回 ROWNUM
  FETCH NEXT 是 12c 才有的，11g 会 ORA-00933，看起来也不像 Oracle。

· 单表 SQL：SELECT 在前，语句铺满右侧
  SELECT 排最前。查询字段和条件字段改成连库那种一行多个。条件带等于/不等于/大于等于。

· 表结构注释能看见，长度不再带 CHAR
  VARCHAR2(100 CHAR) 的 CHAR 是 Oracle 计长单位，不该出现在长度框。AI 改成自增。

· 加载一万行不再把窗口卡死
  一万行 × 很多列全进 DOM 会把窗口卡死。先出遮罩，表格只渲染视口附近。

· 查询结果行号看得见，列宽可拖
  200 列时浏览器把 # 列挤没。改成固定列宽，表头带列序号，右边拖。

· 连库查询勾列回来，底栏按钮不再被裁
  结果卡不能挪到最上面。勾列一直开着。SQL 卡限高，多字段在网格里滚。

· 勾了记住密码，重启应用还在
  Windows 改成本机加密文件，写失败会提示。不再悄悄丢掉。

· 「粘贴 ALTER」改名为「执行 ALTER」

· 测试/本地可跨天记住密码
  连接列表和 cache.db 仍没有密码。生产不记。关掉窗口明天还能连。

· 粘贴 ALTER 放到运行旁边
  中间那张卡再折着也占高度。改成 SQL 卡上的次要按钮，点开弹窗再贴。

· 更新弹窗带上这次改了什么
  以前 latest.json 常只剩「发版 vX」，用户看不见内容。未发版的 CHANGES.md 自动写进说明；弹窗里可滚动。

· 粘贴 ALTER 展开后结果网格横条没了
  右栏 overflow:auto 两轴一起开，展开的 ALTER 把栏撑宽，结果表跟着撑满，横条没了。

· 拿掉界面上的需求确认文案
  范围清单是给评审看的，不该印在连接页和确认框里。按钮和 SQL 还在，只是不再念一遍「做什么、不做什么」。

· 选表下拉可搜索
  生成页、批量、SELECT转UPDATE、多表 JOIN 不再用原生 select 翻清单。

· 连库插入一行（PRD 19.7）
  独立写命令，SQL 框仍然只读。自增列默认不写。不拿复制 INSERT 在本表执行。

· 连库勾选行按主键更新（PRD 19.4）
  独立写命令，SQL 框仍然只读。各行同一套 SET。没主键、没勾字段都不改。

· 生成页在当前连接执行（PRD 19.5）
  单表 / 批量 / SELECT转UPDATE 确认后执行。没连上或方言对不上就禁用。

· 连库勾选行按主键删除（PRD 19.3）
  独立写命令，SQL 框仍然只读。没主键、对不上列、空主键都不删。

· 连库查询写入 PRD §十九，发版 0.9.0
  Oracle / MySQL / PostgreSQL 点表即查；密码不落盘；SQL 框不跑写操作。

· 连库查询 SQL 框铺满右栏宽度

· 连库查询：SQL 在上、结果在下

· 左侧导航收成三组

· 连库工作台：左栏不再空出一截

· 连库表单：列名在输入框前面

· 连库接入 PostgreSQL（共用壳上的第三条方言）
  - 自写 postgres_* 命令，不走 plugin-sql；页面仍用共用壳

· 真页面：我的连接 + 连库进度

· 原型：未连接恢复手填表单

· 原型：连库查询先选「我的连接」

· 原型：我的连接

· 连库后收起左侧菜单给表清单让宽

· 连库 IPC 不再动态 import Tauri 包

· 连库查询独立成一级菜单和工作台
  - 导入页不再嵌「连接数据库」Seg

· 连库后只读 SQL 查询
  - 先 200 行，加载全部最多 1 万；点列头排序、结果内筛选

· 许可码改走 Rust Ed25519
  - 私钥只在 ~/.tauri/chuju-license.key；安装包只有公钥

· macOS 发版补打 .app.tar.gz

· 删掉旧 SqlGenerator，修打包 TS2322

· 文档：11g 连库要 Instant Client，不必装 PL/SQL

· 免费 / 专业版闸门
  - 自导表最多 8 张；SELECT转UPDATE / 多表 / 批量 / 导出 DDL 能看不能拷

· 连库接入 MySQL（共用壳上的第二条方言）
  - 自写 mysql_* 命令，不走 plugin-sql；页面仍用共用壳

· 连库页抽成共用壳，方言只分连接和字典
  - 接入 MySQL 写 src/lib/dbDialects/mysql.ts 并替换 DIALECTS 槽位，不要改 DbConnect.tsx

· Oracle 只读连库导入
  - 官方纯 Rust 驱动，不经过 plugin-sql；密码不落盘

· 打包增加 macOS DMG
  - Mac 上 tauri build 出 dmg；Windows 仍是 NSIS

· SELECT转UPDATE 列名大小写和列数对不上
  - 表头小写对不上大写主键时 WHERE 不再是空的

· 产品显示名改为 CHUJU SQL
  - 窗口、侧栏、安装包显示名对齐；identifier 和缓存 key 不动

· 左侧导航按使用顺序收成一级菜单
  - 日志转SQL、SELECT转UPDATE、我的SQL库仍是独立一级入口

· 批量 INSERT 主键/唯一列连号，不再走自动值
  - 散列自动值会撞 UNIQUE，语句能生成、插不进去

· SELECT转UPDATE 网格粘贴不再被 JSON 里的竖线拆碎
  - 流程实例一类带 JSON 的查询结果不再整行错位

· SELECT转UPDATE 必须先选表再粘贴
  - 有列头按列名对齐，没列头按这张表的字段顺序对齐

· 批量生成正式页拆成 INSERT / UPDATE 两页并加回导航
  - 右栏第一张是生成结果；预览默认折起

· SELECT转UPDATE 正式页按 mockup 重排并加回导航
  - 没贴时只留粘贴；贴完右栏第一张是生成结果，预览默认折起

· 批量生成原型拆成 INSERT / UPDATE 两页
  - 导航「批量生成」作分组；旧 batch-gen.html 转到 INSERT

· 多表关联切菜单再进来会刷新
  - 再点一次当前「多表关联」也当作重新进来

· 产品方言暂时去掉 SQLite
  - 旧模板若存了 sqlite 整条丢掉，避免套用时对不上

· 模板必须点「套用」按钮，点行不再套用
  - 扫一眼名字或点删除附近时，不会把当前配置冲掉

· 多表添加关联时表名不再裁成半截
  - 左表/右表分行；弹层 portal 到 body，长表名折行，表多时可筛选

· 代码块复制仍在顶上，下载和下方复制给长语句
  - 短语句不滚就能复制；正文会在框里滚时脚上再给一次复制

· 日志转SQL「复制全部纯 SQL」按钮上提示已复制
  - 反馈在按钮上，不弹角落 toast

· 日志转SQL 按语句骨架自动选 Oracle / MySQL / PostgreSQL 字面量
  - 反引号按 MySQL 拼日期；NVL/SYSDATE 按 Oracle；ILIKE/:: 按 PostgreSQL

· 日志转SQL 去掉方言切换
  - 字面量固定按 Oracle 拼；顶栏分段和「换成 Oracle / MySQL」一并拿掉

· SQL库长表名不再被裁切
  - 列表名、所属表、按表名筛选都能看见全名

· 日志转SQL 收藏后切到我的SQL库立即可见
  - 各页同时挂着不卸载，收藏只写存储的话 SQL 库仍显示挂载时读到的旧列表

· SQL库右栏按意图互斥：新建或详情，不同时出现
  - 未选中时右侧是新建；点列表后换成详情复制；卡片脚「返回新建」回到表单

· 导航各页书面用语 + 产物优先 + 表头不再挡行
  - 多表模板默认折起；日志对照表默认折起、hero 收短；SQL库右栏先详情复制

· 单表 SQL：语句类型一次只出一种，不再四种 CRUD 混成一段
  - 勾选改成分段选择：点 SELECT 结果里就只有 SELECT，复制出去不用再自己拆

· 编辑表结构弹窗一打开就炸（类型列拆装字段名对不上）
  - toRows 原先把 { base, args } 直接 spread 进行，TypePick 读的是 typeBase，

· 我的SQL库：粘贴 SQL 即识别表名
  - 所属表名跟着 SQL 走（PRD §6.4 / §6.7），手改过表名框后不再覆盖

· 导出 DDL：DEFAULT 放到 NOT NULL 前面
  - columnParts 统一成「类型 → DEFAULT/IDENTITY → NOT NULL → 注释」

· 日志转SQL 结果卡一键收藏 · 「我的SQL库」恢复进导航
  - 每张「解析结果」卡的按钮排加「⭐ 收藏」：整卡语句存成一条收藏，

· 修云端发布构建：@tauri-apps/api 补成直接依赖
  - UpdateButton.tsx 动态 import('@tauri-apps/api/app')，但 package.json 里只有

· 修导入：全引号形态的 Oracle DDL 整批报废（导出→再导入的回路是断的）
  - node-sql-parser 的 mysql 语法把

· 编辑表结构弹窗实时生成增量 DDL（ALTER TABLE）
  - 弹窗字段表格下方新增「增量 DDL」区：拿打开弹窗时的字段和当前编辑实时 diff，

· 菜单暂只留三条：导入表结构 / 查看表结构 / 日志转SQL
  - 生成SQL / SQL语句转换 / 批量生成SQL / 我的SQL库 从左侧菜单拿掉（用户要求

· 编辑表结构：类型列加常用类型下拉 · 补种子 Java 类名显式赋值
  - 类型输入框旁常驻 ▾，点开弹常用类型清单，点选即填；照旧可以手输任意类型

· 导入页「干净态」改版：初始全空 · 导入后结果留页
  - 初始态：输入框不再预填 Oracle 示例，右栏换成虚线空态占位（.empty-hero），

· Oracle 导出 DDL：主键走独立的 alter table add constraint
  - 以前主键只以内联 PRIMARY KEY (id) 写在 CREATE TABLE 体内：约束不会丢，

· 示例表名统一大写 ORDER（速查页两张订单表的根因）· 速查卡加导入日期
  - 根因：缓存按名字原文区分大小写。种子叫小写 order、Oracle 示例导进来是

· 导航重排：配置管理暂退，七页改名并按使用顺序排列
  - 配置管理从导航拿掉（功能代码保留，PAGES 数组加回条目即恢复，

· 导入页：去掉「导入之前你该知道的」，建表输入框伸到底
  - 删「导入之前你该知道的」卡：三条说明里两条已在别处出现

· 修 bug9 速查残留内置表 · bug10 表结构可删除
  - 新增 storage/builtinSeed.ts 启动同步：凭「种子时间戳 + 退役名单」清掉

· 全项目示例只留「订单 + 订单明细」
  - 种子数据、导入页两段 DDL、日志转SQL 的 5 条样例、正则测试预设里的表名统一成

> Windows 安装包（推荐）：`CHUJU SQL_1.2.0_x64-setup.exe` · 含 Oracle 11g 客户端：GitHub Release `v1.2.0` 附件 `CHUJU-SQL_1.2.0_x64-setup-oracle11g.exe`（超 100MB，不进 git） · 便携版：`CHUJU-SQL_1.2.0_portable.exe`（免安装；不含自动更新）
> macOS：`CHUJU SQL_1.2.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v1.1.0 —— 2026-08-31

发版 v1.1.0

> Windows 安装包（推荐）：`CHUJU SQL_1.1.0_x64-setup.exe` · 含 Oracle 11g 客户端：`CHUJU SQL_1.1.0_x64-setup-oracle11g.exe`（本次未打） · 便携版：`CHUJU-SQL_1.1.0_portable.exe`（免安装；不含自动更新）
> macOS：`CHUJU SQL_1.1.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v1.0.0 —— 2026-08-31

发版 v1.0.0

> Windows 安装包：`CHUJU SQL_1.0.0_x64-setup.exe` · 便携版：`CHUJU-SQL_1.0.0_portable.exe`（免安装，直接运行；不含自动更新，需手动覆盖升级）
> macOS：`CHUJU SQL_1.0.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v0.9.0 —— 2026-08-30

发版 v0.9.0

> Windows 安装包：`CHUJU SQL_0.9.0_x64-setup.exe` · 便携版：`CHUJU-SQL_0.9.0_portable.exe`（免安装，直接运行；不含自动更新，需手动覆盖升级）
> macOS：`CHUJU SQL_0.9.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v0.8.0 —— 2026-08-30

发版 v0.8.0

> Windows 安装包：`CHUJU SQL_0.8.0_x64-setup.exe` · 便携版：`CHUJU-SQL_0.8.0_portable.exe`（免安装，直接运行；不含自动更新，需手动覆盖升级）
> macOS：`CHUJU SQL_0.8.0_aarch64.dmg`（拖进应用程序；未签名时需右键 → 打开）## v0.6.0 —— 2026-08-28

fix: @tauri-apps/api 补成直接依赖——修云端构建 TS2307

- UpdateButton.tsx 动态 import('@tauri-apps/api/app')，但 package.json 里只有
  三个 @tauri-apps/plugin-*，api 一直是传递依赖。pnpm 严格布局不会把它链到
  项目根 node_modules，云端干净 checkout 的 tsc -b 当场报 TS2307 找不到模块
- 本地四道闸门为什么一直全绿：C:\Users\pdw\node_modules（用户主目录）里有
  一份野生的 @tauri-apps/api，TS 沿父目录找 node_modules 时捡到了，把缺依赖
  掩住了。这份野的换台机器就没有——不能当依赖用，只能当陷阱
- pnpm add @tauri-apps/api@^2.11.1：与传递依赖同版本（2.11.1），./app 走
  exports 的 ./* 通配解析到 app.d.ts；require.resolve 已落在项目自己的
  .pnpm store 里，不再经过主目录

> 便携版：`DevTools_0.6.0_portable.exe`（免安装，直接运行；不含自动更新，需手动覆盖升级）

# 更新日志
## v0.4.0 —— 2026-08-27

新增免安装包

> 便携版：`DevTools_0.4.0_portable.exe`（免安装，直接运行；不含自动更新，需手动覆盖升级）
## v0.3.0 —— 2026-08-27

新增select转update，批量生成功能

> 便携版：`DevTools_0.3.0_portable.exe`（免安装，直接运行；不含自动更新，需手动覆盖升级）## v0.2.0 —— 2026-08-26

新增配置管理页：YAML/Properties 一键互转保留注释、实时校验报行号、智能提示；内置自动更新
